export type StudioView = "create" | "productions" | "library" | "settings"

export const VIEW_LABELS: Record<StudioView, string> = {
  create: "Create",
  productions: "Productions",
  library: "Media library",
  settings: "Settings",
}
